@model IEnumerable<ProjektBiblioteka.Models.Kategoria>

@{
    ViewData["Title"] = "Kategorie";
}

<h1>Kategorie</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nazwa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.KategoriaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.KategoriaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.KategoriaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
